# Core
torch==2.12.0
torchvision==0.27.0
numpy==2.5.0
opencv-python==5.0.0.93
pillow==12.2.0

# HuggingFace (ONNX export & text encoding)
transformers==5.12.1

# SAM3 dependencies
einops==0.8.2
ftfy==6.1.1
regex==2026.5.9
iopath==0.1.10

# Export
onnx==1.21.0

# System dependencies (not pip):
#   ffmpeg          — video restream (ds9_video.py)
#   DeepStream 9    — pyservicemaker, pyds, GStreamer plugins deepstream-9.0_9.0.0-1_amd64.deb
#   TensorRT 10.x   — trtexec, libnvinfer (NVIDIA installer)
#   CUDA 13.0       — torch installed with +cu130 index

# SAM3 (Meta) — install from source:
#   pip install git+https://github.com/facebookresearch/sam3.git
